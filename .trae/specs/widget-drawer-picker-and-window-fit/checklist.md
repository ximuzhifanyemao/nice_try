# Checklist

- [x] 点击带学习内容的科目：抽屉从窗口底部滑入，覆盖式展示该科目全部学习内容，无需滚动
- [x] 点击抽屉内任一项：立即开始计时，抽屉自动收起，科目列表布局不被推动
- [x] 点击遮罩 / 关闭按钮 / 按 Esc：抽屉收起，不产生计时操作
- [x] 点击无学习内容的科目：直接开始计时，不弹抽屉（现有行为保持）
- [x] 收缩到 380×520 抽屉显示完整、无滚动条、无裁切，样式与新版深色玻璃风格一致
- [x] 从精简切「全部功能」：窗口尺寸钳制在当前显示器工作区内并居中，完整可见、不溢出屏幕
- [x] 工作区足够大时仍按 1600×1000 展开并居中
- [x] 「精简靠右 → 全部功能 → 精简计时」全程：收回后精简窗口回到先前靠右位置，不被切换中间位置覆盖
- [x] onMoved 在模式切换期间不向 localStorage 写入位置；切换后恢复持久化
- [x] `npm run build:deploy` 通过，无 TypeScript 类型错误
- [x] 代码走查：原流式活动面板渲染分支已移除，无残留死代码