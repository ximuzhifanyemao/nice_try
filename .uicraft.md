# DiveDeep 设计系统

## 设计方向：专注沉浸

考研学习工具，核心目标是帮助用户保持专注、减少干扰。视觉设计以此为核心：
- 深色为主，降低视觉疲劳
- 数据优先：计时、进度、倒计时数据是视觉焦点
- 导航退后：导航元素低饱和、小尺寸，不抢主内容
- 动效克制：仅在状态变化时给出反馈

## 配色规范

### 背景
- 主背景（桌面/暗色）：`bg-slate-950`（比 slate-900 更深）
- 卡片表面：`bg-slate-900`（桌面）/ `bg-white`（移动亮色）
- 亮色主背景：`bg-gray-50`

### 强调色
- 主强调：`indigo-500`（计时、进度条、激活态）
- 次强调：`violet-500`（成就、特殊数据）

### 文字层次
| 层级 | 暗色 | 亮色 | 用途 |
|------|------|------|------|
| 主文字 | text-slate-100 | text-gray-900 | 标题、数据 |
| 次文字 | text-slate-400 | text-gray-500 | 描述、标签 |
| 弱文字 | text-slate-500 | text-gray-400 | 辅助、时间戳 |
| 极弱 | text-slate-600 | text-gray-300 | 占位、禁用 |

### 科目配色（保持不变）
- math: blue | english: green | 408: purple | politics: red

## 间距规范

- 基准单位：4px
- 紧凑间距：`gap-1.5`（6px）— 科目按钮之间
- 标准间距：`gap-3`（12px）— 卡片内元素
- 卡片间距：`gap-2`（8px）— 移动端卡片网格
- 页面内边距：`px-4 py-3`（移动）/ `px-6 py-4`（桌面）

## 字体规范

- 计时数字：`text-5xl font-bold tabular-nums` — 大、等宽、醒目
- 页面标题：`text-xl font-bold`
- 卡片标题：`text-sm font-semibold`
- 辅助文字：`text-xs`
- 按钮文字：`text-sm font-medium`

## 圆角规范

- 卡片：`rounded-xl`（12px）
- 按钮：`rounded-lg`（8px）
- 小元素（tag/chip）：`rounded-md`（6px）
- 挂件窗口：`rounded-xl`

## 动效规范

- 状态切换：`transition-colors duration-200`
- 位置变化：`transition-all duration-200`
- 激活脉冲：计时中用 `animate-pulse` 在状态指示上
- 页面切换：无额外动画，保持即时响应
- reduced-motion：`motion-reduce:transition-none`

## 组件规范

### 卡片
- 暗色：`bg-slate-900 border border-slate-800 rounded-xl`
- 亮色：`bg-white border border-gray-200 rounded-xl shadow-sm`

### 按钮
- 主按钮：`bg-indigo-600 hover:bg-indigo-500 text-white rounded-lg`
- 次按钮：`bg-slate-800 hover:bg-slate-700 text-slate-200 rounded-lg`
- 危险按钮：`bg-red-600 hover:bg-red-500 text-white rounded-lg`
- 幽灵按钮：`text-slate-400 hover:text-slate-200 hover:bg-slate-800/50`

### 导航
- 侧边栏宽：`w-16`（图标）/ `w-20`（图标+文字）
- 底部 Tab 高：`h-14`
- 激活态：左侧 2px 竖线 + 背景色变化
